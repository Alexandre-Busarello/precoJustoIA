# Configuração para processos em background
# Esta configuração deve ser usada apenas para scripts de background como fetch-data-ward.ts

# String de conexão com pool limitado para processos em background
# Limita o número máximo de conexões para 3 para não impactar a aplicação principal
BACKGROUND_PROCESS_POSTGRES="postgresql://usuario:senha@localhost:5432/analisador_acoes?connection_limit=3&pool_timeout=20"

# Outras variáveis necessárias para os scripts
WARD_JWT_TOKEN="seu_token_ward_aqui"
BRAPI_TOKEN="seu_token_brapi_aqui"
GEMINI_API_KEY="sua_chave_gemini_aqui"

# Configurações opcionais para controle de performance
# MAX_CONCURRENT_OPERATIONS=2  # Máximo de operações simultâneas (padrão: 2)
# BATCH_DELAY_MS=200           # Delay entre operações em ms (padrão: 200)
